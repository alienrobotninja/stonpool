server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # static assets (incl. tonconnect-manifest.json and icon-180.png) resolve directly;
  # everything else falls through to the SPA entry
  location / {
    try_files $uri $uri/ /index.html;
  }
}